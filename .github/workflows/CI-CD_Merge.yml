name: On Merge

on: 
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Dotnet Build
      run: dotnet build --configuration Release ./CsharpLibs/Garciss.Csharp.Libs.sln

    - name: Generar nuspec
      run: |
        ls NugetPackages/PackagesCompilaciones/netstandard2.1
        cd NugetPackages/NugetConfig/
        .\nuspecGeneratorNetCoreNetStandard.ps1
      shell: powershell
    

    - name: Add private GitHub registry to NuGet
      run: nuget source Add -Name "PackagesGitHub" -Source "https://api.nuget.org/v3/index.json" -UserName ${{ secrets.USERNAME_GITHUB }} -Password ${{ secrets.USER_TOKEN_GITHUB_PACKAGE }}

    - name: Nuget Pack para preparar el paquete
      run: |
        ls NugetPackages/PackagesCompilaciones/netstandard2.1
        cd NugetPackages/NugetConfig/
        nuget pack Garciss.Csharp.libs.nuspec -Verbosity Detail
        
    - name: Push generated package to GitHub registry
      run: |
        cd NugetPackages/NugetConfig/
        nuget push *.nupkg -Source "PackagesGitHub" -SkipDuplicate
        
